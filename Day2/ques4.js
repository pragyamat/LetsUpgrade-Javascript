// Question-4
//Program to display only elements containing 'a' in them from a array 
let names = ["George","Madeline","Ramesh","Avantika"];
for(let i=0 ; i<names.length ; i++)
{
    if( names[i].includes("a"))
        {
            console.log(names[i]);
        }
}
